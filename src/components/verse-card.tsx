"use client";

// ... existing imports ...
import { WordTag } from "@/components/word-tag";
import { VerseTag } from "@/components/verse-tag";

// Update all instances of WordTag and VerseTag to use the new components
// Make sure to pass the correct props and handlers
// (The main logic remains the same, just use the new styled components)